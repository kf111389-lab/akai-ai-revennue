import React from 'react';

interface ThemeSwitcherProps {
  variant?: 'navbar' | 'floating';
}

export const ThemeSwitcher: React.FC<ThemeSwitcherProps> = () => {
  // Theme is locked permanently to Forest & Honey Gold
  return null;
};

